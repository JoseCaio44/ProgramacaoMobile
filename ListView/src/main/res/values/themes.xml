<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Prova" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="textStyle">
        <!--<item name="android:textColor">@color/white</item> -->
        <item name="android:typeface">monospace</item>
    </style>
    <style name="Title" parent="textStyle">
        <item name="android:textSize">25sp</item>
    </style>
    <style name="backgroung" parent="Theme.Prova">
        <item name="background">@color/back</item>
    </style>
    <!-- Estilo do botão -->
    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/um</item>
    </style>

    <style name="Theme.Prova" parent="Base.Theme.Prova" />

</resources>